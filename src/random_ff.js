function vrandom_pos_ff(){
    a=1;
    b=6;
    r1= (a+Math.floor(Math.random()*b));

    document.getElementById('f1').value = r1;
    r2= (a+Math.floor(Math.random()*b));
    document.getElementById('f2').value = r2;

    r3= (a+Math.floor(Math.random()*b));
    document.getElementById('f3').value = r3;

    r4= (a+Math.floor(Math.random()*b));
    document.getElementById('f4').value = r4;

    r5= (a+Math.floor(Math.random()*b));
    document.getElementById('f5').value = r5;

    r6= (a+Math.floor(Math.random()*b));
    document.getElementById('f6').value = r6;

    r7= (a+Math.floor(Math.random()*b));
    document.getElementById('f7').value = r7;

    r8= (a+Math.floor(Math.random()*b));
    document.getElementById('f8').value = r8;

    r9= (a+Math.floor(Math.random()*b));
    document.getElementById('f9').value = r9;
}